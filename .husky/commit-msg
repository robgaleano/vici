#!/usr/bin/env sh

npx --no -- commitlint --edit "$1"
status=$?

if [ "$status" -ne 0 ]; then
	echo ""
	echo "Suggestion: use 'git commit' without '-m' or 'pnpm commit' to open the interactive czg flow."
	echo "Valid examples: feat(setup): ... | fix(lint): ... | chore(git): ..."
fi

exit "$status"